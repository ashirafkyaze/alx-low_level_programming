#include "main.h"

/**
 * swap_int - paramater that swaps the values of two integers.
 * @a: input integer.
 * @b: input integer.
 * Return: no return  value.
 */

void swap_int(int *a, int *b)
{
	int *n;

	*a = *n;
	*b = *a;
	*n = *b;
}
