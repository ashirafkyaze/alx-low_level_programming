#include "main.h"

/**
 * _isdigit - checks if parameter is a number between 0 to 9.
 * @c: input number.
 * Returns 1 if c is a number
 * Returns 0 otherwise
 */

int _isdigit(int c)
{
	int i;

	i = 0;

	while (i <= 9)
	{
		if (x == i)
		{
			return (1);
		}
		else
		{
			i++;
		}
	}
	return (0);
}
